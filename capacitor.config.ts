import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zyvionix.motorcycledealer',
  appName: 'Motorcycle Dealer',
  webDir: 'out',
};

export default config;